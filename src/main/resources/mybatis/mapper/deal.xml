<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="deal"> 
	<delete id="deleteProduct" parameterType="string">
		DELETE FROM tbl_deal
			  WHERE product_id =#{productId}
	</delete>
	
	<insert id="addDeal" parameterType="map">
		INSERT INTO tbl_deal
  			(deal_id, product_id, purchaser_uid, user_name, phone_num, deal_means, safe_deal, direct_place, zipcode, address)
  		VALUES
  			(#{dealId}, #{productId}, #{purchaserUid}, #{userName}, #{phoneNum}, #{dealMeans}, #{safeDeal}, #{directPlace}, #{zipcode}, #{address})
	</insert>
</mapper>